management:
  health:
    probes:
      enabled: true
  endpoint:
    health:
      show-details: always
      show-components: always
  endpoints:
    web:
      exposure:
        include: 'info,health,live,ready'
      base-path: /.mystatus